# Probado con Python 3.10.9 en Windows 11.
librosa>=0.11.0      # pulso, ataques, CQT, segmentacion
numpy>=2.0
scipy>=1.11
soundfile>=0.13      # lectura de ogg/flac/wav
mido>=1.3            # lectura de notes.mid para minar el corpus

# OPCIONAL, para el karaoke en ingles. 6 MB, Python puro, sin binarios.
# Medido contra los cortes de silaba de 99 canciones humanas de la biblioteca:
# sin pyphen 66.8 % de palabras exactas, con pyphen 81.7 %; y la precision de
# las fronteras sube de 51.6 % a 84.7 %. Sin el, silabas.py usa su heuristico.
pyphen>=0.15

# ffmpeg tiene que estar en el PATH para los mp3.
# Comprobar con:  ffmpeg -version
